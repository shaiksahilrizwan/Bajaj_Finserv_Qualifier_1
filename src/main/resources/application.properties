# Application configuration
spring.application.name=webhook-sql-solver
server.port=8080

# Logging configuration
logging.level.com.webhook.solver=INFO
logging.level.org.springframework.web.client=DEBUG